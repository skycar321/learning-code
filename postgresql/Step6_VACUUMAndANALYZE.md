# PostgreSQL Step 6: VACUUM �?ANALYZE ?�해?� ?�용
# MVCC(Multi-Version Concurrency Control)?� `VACUUM`???�요?? ?�동 VACUUM 모니?�링 �?`ALTER TABLE`???�한 ?�이블별 ?�닝

# ?�쁜 ?�시: `VACUUM`??중요?�을 간과?�여 ?�이블이 비�??��?�? 쿼리 ?�능???�?�되�? ?�덱?��? 비효?�적?�로 ?�용?�도�?방치?�니??
# 좋�? ?�시: MVCC???�동 방식???�해?�고, `VACUUM`�?`ANALYZE` 명령?��? ?�절???�용?�여 ?�이블을 최적?�하�? ?�동 VACUUM ?�정??`ALTER SYSTEM` ?�는 `ALTER TABLE`???�해 ?�크로드??맞춰 ?�닝?�여 ?�이?�베?�스??건강???�태�??��?.

# ?�습 ?�인?? PostgreSQL??MVCC ?�키?�처???�시???�어�?가?�하�??��?�? �??�가�?주기?�인 `VACUUM` ?�업???�수?�입?�다. 직접?�인 `postgresql.conf` ?�정???�려???�경?�서??SQL 명령?��? ?�한 ?�정 조정??중요?�니??

---

## 1. MVCC (Multi-Version Concurrency Control)?� `VACUUM`???�요??
### 1.1. MVCC
- PostgreSQL?� MVCC(?�중 버전 ?�시???�어) 모델???�용?�니??
- `UPDATE` ?�는 `DELETE` ?�업??발생?�면, ?�제 ?�을 즉시 ??��?�거????��?��? ?�고 ?�로??버전???�을 ?�성?�니??
- `UPDATE`??`INSERT`?� `DELETE`??조합?�로 ?�동?�니?? (기존 ?��? ??�� ?�시, ?????�입)
- ?�렇�??�으로써 ?�기 ?�업(SELECT)?� ?�기 ?�업(UPDATE/DELETE)???�해 블록?��? ?�고, ??�� ?�랜??�� ?�작 ?�점???��????�냅?�을 �????�습?�다.

### 1.2. ?�드 ?�플 (Dead Tuples)
- MVCC�??�해 `UPDATE` ?�는 `DELETE`???�의 ?�전 버전?� "?�드 ?�플"�??�게 ?�니??
- ?�드 ?�플?� ?�스??공간??차�??�며, ?�이�??�캔 ??불필?�하�??�히므�?쿼리 ?�능???�?�시?�니??
- ?�드 ?�플??많아지�??�덱?�도 비�??�져 비효?�적?�로 ?�니??

### 1.3. `VACUUM`????��
- `VACUUM` 명령?�는 ?�드 ?�플??차�??�는 ?�스??공간??"?�활??가???�게 ?�시?�니??
- **`VACUUM`**: ?�드 ?�플??차�??�는 공간???�활??가?�하�??�시?��?�? ?�일 ?�기�?줄이지???�습?�다.
- **`VACUUM FULL`**: ?�이�??�체�??�작?�하???�드 ?�플???�전???�거?�고 ?�스??공간???�영체제�?반환?�니?? **매우 ?�리�??�이�??�을 ?�발?��?�??�로?�션?�서??거의 ?�용?��? ?�습?�다.**
- **`VACUUM (FREEZE)`**: ?�랜??�� ID 롤오�?Transaction ID Wraparound) 문제�?방�??�니?? ?�래???�랜??�� ID가 모두 ?�거?�면 ?�로 ?�성?�는 ?�랜??�� ID가 ?�시 ?�작?�어 무한??증�??�는 것을 방�??�니??

## 2. `ANALYZE`????��

- **??��**: ?�이블의 ?�용(?�이??분포)???�???�계 ?�보�??�집?�고 갱신?�니??
- ???�계 ?�보??쿼리 ?�티마이?�가 최적???�행 계획???�성?�는 ???�용?�니??
- `ANALYZE`가 ?�행?��? ?�으�??�티마이?�???�래???�계�?기반?�로 비효?�적???�행 계획???�택?????�습?�다.
- `VACUUM`?� ?�드 ?�플??처리?�고, `ANALYZE`???�계 ?�보�?갱신?�니?? ???��? ?�로 ?�른 목적??가집니??

## 3. `autovacuum` (?�동 VACUUM)

- PostgreSQL?� 백그?�운?�에??`autovacuum` ?�몬???�행?�여 `VACUUM` �?`ANALYZE` ?�업???�동?�로 ?�행?�니??
- **`autovacuum = on`?� ?�수?�니??** (기본�?
- **?�닝 ?�라미터**: `ALTER SYSTEM` ?�는 `ALTER TABLE` 명령?��? ?�용?�여 `autovacuum` 관???�라미터?�을 조정?�여 ?�크로드??맞게 ?�동 VACUUM???�작??최적?�할 ???�습?�다.

### 3.1. 주요 ?�동 VACUUM ?�닝 ?�라미터

#### `autovacuum_vacuum_scale_factor` / `autovacuum_vacuum_threshold`
- **??��**: ?�이블의 `VACUUM`???�리거하??조건. (?�드 ?�플??비율 ?�는 개수)
- **가?�드?�인**:
    - `autovacuum_vacuum_scale_factor` (기본�? 0.2): ?�드 ?�플 비율 (?�이�??�기 ?��?20%).
    - `autovacuum_vacuum_threshold` (기본�? 50): ?�드 ?�플 ?��? 개수.
    - `VACUUM`?� `(?�드_?�플_개수 > autovacuum_vacuum_threshold + autovacuum_vacuum_scale_factor * �??�플_개수)`?????�행?�니??
- **?�닝**: `INSERT/UPDATE/DELETE`가 많�? ?�이블�? `scale_factor`�???��거나 `threshold`�??�여 `VACUUM`?????�주 ?�행?�도�?조정?????�습?�다. (?�이블별 ?�정 권장)

#### `autovacuum_analyze_scale_factor` / `autovacuum_analyze_threshold`
- **??��**: ?�이블의 `ANALYZE`�??�리거하??조건. (변경된 ?�의 비율 ?�는 개수)
- **가?�드?�인**: `autovacuum_analyze_scale_factor` (기본�? 0.1), `autovacuum_analyze_threshold` (기본�? 50).
- **?�닝**: ?�이??분포가 ?�주 변경되???�이블�? `scale_factor`�???�� `ANALYZE`�????�주 ?�행?�도�?조정?????�습?�다. (?�이블별 ?�정 권장)

#### `autovacuum_vacuum_cost_delay`
- **??��**: ?�동 VACUUM???�업???�시 중�??�는 ?�간 (밀리초).
- **가?�드?�인**: `20ms` (기본�?. ?�무 ??���?VACUUM???�무 공격?�으�??�행?�어 ?�스???�원??많이 ?�용?�고, ?�무 ?�으�?VACUUM???�려집니?? I/O 부?��? ?��? ?�스?�에?�는 ?�게 ?�정?�여 VACUUM??백그?�운?�에??조용???�행?�도�??????�습?�다.

#### `autovacuum_max_workers`
- **??��**: ?�시???�행?????�는 최�? ?�동 VACUUM ?�커 ?�로?�스 ??
- **가?�드?�인**: `3` (기본�?. CPU 코어 ?��? ?�스??I/O ?�??��??고려?�여 조정.

### 3.2. ?�동 VACUUM ?�정 변�?방법

- **?�역 ?�정 (DB ?�버 ?�체)**: `ALTER SYSTEM SET autovacuum_vacuum_cost_delay = '10ms';`
  - 변�???`SELECT pg_reload_conf();`�??�행?�면 ?�시???�이 ?�용?�니??
- **?�이블별 ?�정**: `ALTER TABLE <table_name> SET (autovacuum_vacuum_scale_factor = 0.05, autovacuum_vacuum_threshold = 100);`
  - ?�이블별 ?�정?� ?�역 ?�정????��?�니?? `INSERT/UPDATE/DELETE` ?�턴??매우 ?�른 ?�이블에 ?�용?�니??

## 4. ?�동 VACUUM �?ANALYZE

- `VACUUM <table_name>;`
- `ANALYZE <table_name>;`
- `VACUUM (VERBOSE, ANALYZE) <table_name>;` (?�세 ?�보 ?�함)
- `REINDEX TABLE <table_name>;` ?�는 `REINDEX INDEX <index_name>;` (?�덱???�구??

## 5. ?�랜??�� ID 롤오�?(Transaction ID Wraparound)

- PostgreSQL???�랜??�� ID??32비트 ?�수?�며, ??20??개�? ?�용?�면 ?�시 0?�로 ?�아갑니??
- ?�드 ?�플???�무 ?�래 ?�아?�어 ?�랜??�� ID가 롤오버되�??�이???�실??방�??�기 ?�해 ?�이?�베?�스가 ?�다?�될 ???�습?�다.
- `VACUUM (FREEZE)`????문제�?방�??�니?? ?�동 VACUUM?????�업???�동?�로 ?�행?�니??
- `autovacuum_freeze_max_age` (기본�? 2?? ?�라미터??`FREEZE`�??�리거하???�랜??�� ID??최�? 개수�??�정?�니?? (?�역 ?�정?� `ALTER SYSTEM`, ?�이블별 ?�정?� `ALTER TABLE`�?가??

---

```
-- ???�일?� VACUUM �?ANALYZE??개념???�명?�고 SQL 명령?��? ?�한 ?�닝 방법???�시?�니??
-- `ALTER SYSTEM` ?�는 `ALTER TABLE` 명령?��? ?�용?�여 ?�동 VACUUM ?�정??조정?????�습?�다.

-- ?�드 ?�플 �?ANALYZE ?�보 ?�인 (SQL �??�용)
SELECT
    relname,
    n_live_tup, -- ?�아?�는 ?�플 ??    n_dead_tup, -- ?�드 ?�플 ??    last_autovacuum,
    last_autoanalyze
FROM pg_stat_all_tables
WHERE schemaname = 'public'
ORDER BY n_dead_tup DESC, n_mod_since_analyze DESC;

-- ?�이블의 ?�동 VACUUM ?�정 ?�인
SELECT relname, reloptions FROM pg_class WHERE relname = 'your_table_name';

-- ?�정 ?�이블에 ?�???�동 VACUUM ?�정 변�?(?�시)
-- ALTER TABLE your_table SET (autovacuum_vacuum_scale_factor = 0.05);
-- ALTER TABLE your_table SET (autovacuum_vacuum_threshold = 100);
-- ALTER TABLE your_table SET (autovacuum_analyze_scale_factor = 0.02);
```

