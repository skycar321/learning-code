# Super Claude 기능 가이드: 효과적인 사용법 및 예시

이 문서는 Super Claude CLI의 다양한 기능을 소개하고, 각 기능을 어떤 상황에서 어떻게 활용하면 좋을지에 대한 상세한 가이드라인과 예시를 제공합니다. Super Claude는 복잡한 소프트웨어 엔지니어링 작업을 효율적으로 수행하도록 설계된 강력한 도구 모음입니다.

---

## 1. 개요: Super Claude의 핵심 가치

Super Claude는 `devops`, `frontend`, `backend`, `security`, `qa`, `project-manager`, `architect`, `analyzer`, `mentor` 등 다양한 전문가 페르소나와 `Sequential`, `Context7`, `Magic`, `Playwright`, `Morphllm`, `Serena`와 같은 전문 MCP(Multi-Computational Persona) 서버를 통합하여 복잡한 작업을 자동화하고 최적화합니다.

주요 목표는 다음과 같습니다:
- **자동화된 오케스트레이션**: 복잡한 워크플로우를 자동화하고 여러 도구와 에이전트를 조율합니다.
- **컨텍스트 인식**: 프로젝트 컨텍스트를 유지하고 과거 학습을 활용하여 더 정확한 작업을 수행합니다.
- **생산성 향상**: 반복적인 작업을 줄이고 개발자가 핵심 문제 해결에 집중하도록 돕습니다.
- **품질 보증**: 코드 품질, 보안 및 성능을 체계적으로 검증합니다.

---

## 2. Super Claude 핵심 기능 가이드

`/sc:` 접두사를 사용하여 모든 Super Claude 명령에 액세스할 수 있습니다.

### 2.1 `/sc:analyze` - 코드 분석 및 품질 평가

**설명**: 코드 품질, 보안, 성능 및 아키텍처 도메인 전반에 걸쳐 포괄적인 코드 분석을 수행합니다.
**활용 시기**:
- 프로젝트 또는 특정 구성 요소에 대한 코드 품질 평가가 필요할 때.
- 보안 취약점 스캔 및 규정 준수 검증이 필요할 때.
- 성능 병목 현상을 식별하고 최적화 계획을 수립할 때.
- 아키텍처 검토 및 기술 부채 평가가 필요할 때.
**예시**:
- **전체 프로젝트 분석**: `/sc:analyze` (전체 프로젝트의 다중 도메인 분석을 수행하고 주요 결과 및 로드맵이 포함된 포괄적인 보고서를 생성합니다.)
- **인증 구성 요소의 심층 보안 분석**: `/sc:analyze src/auth --focus security --depth deep` (인증 구성 요소의 심층 보안 분석을 수행하고 상세한 수정 지침이 포함된 취약점 평가를 제공합니다.)
- **성능 병목 현상 식별**: `/sc:analyze --focus performance --format report` (성능 병목 현상을 식별하고 최적화 권장 사항이 포함된 HTML 보고서를 생성합니다.)

### 2.2 `/sc:brainstorm` - 대화형 요구사항 탐색

**설명**: 소크라테스식 대화와 체계적인 탐색을 통해 요구사항을 발견하고 명확히 합니다.
**활용 시기**:
- 모호한 프로젝트 아이디어를 구조화된 방식으로 탐색해야 할 때.
- 요구사항을 발견하고 구체적인 사양으로 발전시켜야 할 때.
- 개념을 검증하고 실현 가능성을 평가해야 할 때.
- 교차 세션 브레인스토밍 및 반복적인 개선이 필요할 때.
**예시**:
- **AI 기반 프로젝트 관리 도구에 대한 체계적인 탐색**: `/sc:brainstorm "AI-powered project management tool" --strategy systematic --depth deep` (아키텍처, 분석, 프로젝트 관리자 페르소나를 활용하여 체계적인 탐색 프레임워크를 제공합니다.)
- **실시간 협업 기능에 대한 애자일 탐색**: `/sc:brainstorm "real-time collaboration features" --strategy agile --parallel` (프론트엔드, 백엔드, 보안 페르소나를 통해 병렬 탐색 경로를 제공합니다.)

### 2.3 `/sc:build` - 프로젝트 빌드 및 패키징

**설명**: 지능형 오류 처리 및 최적화를 통해 프로젝트를 빌드, 컴파일 및 패키징합니다.
**활용 시기**:
- 다른 환경에 대한 프로젝트 컴파일 및 패키징 요청이 있을 때.
- 빌드 최적화 및 아티팩트 생성이 필요할 때.
- 빌드 프로세스 중 오류 디버깅이 필요할 때.
- 배포 준비 및 아티팩트 패키징이 필요할 때.
**예시**:
- **표준 프로젝트 빌드**: `/sc:build` (기본 구성을 사용하여 전체 프로젝트를 빌드하고 아티팩트 및 포괄적인 빌드 보고서를 생성합니다.)
- **최적화된 프로덕션 빌드**: `/sc:build --type prod --clean --optimize` (고급 최적화를 통해 클린 프로덕션 빌드를 수행합니다. 축소, 트리 셰이킹 및 배포 준비를 포함합니다.)
- **Playwright를 사용한 개발 빌드 유효성 검사**: `/sc:build --type dev --validate` (UI 테스트 및 빌드 검증 통합을 위한 Playwright 유효성 검사가 포함된 개발 빌드를 수행합니다.)

### 2.4 `/sc:business-panel` - 비즈니스 패널 분석 시스템

**설명**: 저명한 비즈니스 사상가들이 각자의 프레임워크와 방법론을 통해 문서를 분석하는 AI 지원 패널 토론입니다.
**활용 시기**:
- 비즈니스 전략, 시장 분석, 혁신 등 다양한 비즈니스 문서에 대한 다각적인 전문가 의견이 필요할 때.
- 특정 비즈니스 문제에 대한 심층적인 분석과 솔루션 제안이 필요할 때.
- 전문가 간의 토론, 비판, 질문을 통해 아이디어를 스트레스 테스트하거나 학습을 촉진할 때.
**예시**:
- **기본 비즈니스 문서 분석**: `/sc:business-panel [document_path_or_content]` (기본 토론 모드에서 문서 내용을 분석합니다.)
- **특정 전문가 및 토론 모드**: `/sc:business-panel [content] --experts "porter,christensen,meadows" --mode debate` (선택된 전문가들이 특정 비즈니스 내용을 토론 모드로 분석하게 합니다.)
- **전략적 질문 생성**: `/sc:business-panel [content] --mode socratic --questions` (소크라테스 모드로 전환하여 문서를 통해 핵심 전략적 질문을 도출합니다.)

### 2.5 `/sc:cleanup` - 코드 및 프로젝트 정리

**설명**: 코드를 체계적으로 정리하고, 사용되지 않는 코드를 제거하고, 프로젝트 구조를 최적화합니다.
**활용 시기**:
- 코드 유지 관리 및 기술 부채 감축 요청이 있을 때.
- 사용되지 않는 코드 제거 및 import 최적화가 필요할 때.
- 프로젝트 구조 개선 및 구성 요구 사항이 있을 때.
- 코드베이스 위생 및 품질 개선 이니셔티브가 있을 때.
**예시**:
- **안전한 코드 정리**: `/sc:cleanup src/ --type code --safe` (모든 기능을 보존하면서 사용되지 않는 코드를 제거하는 보수적인 정리를 수행합니다.)
- **import 최적화 미리보기**: `/sc:cleanup --type imports --preview` (실행 없이 사용되지 않는 import 정리를 분석하고 표시합니다. Context7 패턴을 통해 프레임워크 인식 최적화를 수행합니다.)
- **포괄적인 프로젝트 정리**: `/sc:cleanup --type all --interactive` (복잡한 결정에 대한 사용자 지침과 함께 다중 도메인 정리를 수행합니다.)

### 2.6 `/sc:design` - 시스템 및 구성 요소 설계

**설명**: 포괄적인 사양을 통해 시스템 아키텍처, API 및 구성 요소 인터페이스를 설계합니다.
**활용 시기**:
- 아키텍처 계획 및 시스템 설계 요청이 있을 때.
- API 사양 및 인터페이스 설계가 필요할 때.
- 구성 요소 설계 및 기술 사양 요구 사항이 있을 때.
- 데이터베이스 스키마 및 데이터 모델 설계 요청이 있을 때.
**예시**:
- **사용자 관리 시스템 아키텍처 설계**: `/sc:design user-management-system --type architecture --format diagram` (구성 요소 관계 및 확장성 고려 사항을 포함하는 포괄적인 시스템 아키텍처를 생성합니다.)
- **결제 API 사양 설계**: `/sc:design payment-api --type api --format spec` (RESTful 설계 원칙 및 산업 표준을 따르는 상세한 API 사양을 생성합니다.)
- **전자 상거래 DB 스키마 설계**: `/sc:design e-commerce-db --type database --format diagram` (엔티티 관계 및 제약 조건을 포함하는 데이터베이스 스키마를 생성합니다. 정규화 및 성능 고려 사항을 포함합니다.)

### 2.7 `/sc:document` - 문서 생성

**설명**: 구성 요소, 함수, API 및 기능에 대한 집중적인 문서를 생성합니다.
**활용 시기**:
- 특정 구성 요소, 함수 또는 기능에 대한 문서 요청이 있을 때.
- API 문서 및 참조 자료 생성이 필요할 때.
- 코드 주석 및 인라인 문서 요구 사항이 있을 때.
- 사용자 가이드 및 기술 문서 생성 요청이 있을 때.
**예시**:
- **인라인 코드 문서**: `/sc:document src/auth/login.js --type inline` (매개변수 및 반환 설명이 포함된 JSDoc 주석을 생성하고 함수 및 클래스에 대한 포괄적인 인라인 문서를 추가합니다.)
- **API 참조 생성**: `/sc:document src/api --type api --style detailed` (엔드포인트 및 스키마가 포함된 포괄적인 API 문서를 생성하고 사용 예시 및 통합 지침을 생성합니다.)
- **사용자 가이드 생성**: `/sc:document payment-module --type guide --style brief` (실용적인 예시와 함께 사용자 중심 문서를 생성하고 구현 패턴 및 일반적인 사용 사례에 중점을 둡니다.)

### 2.8 `/sc:estimate` - 개발 예측

**설명**: 지능형 분석을 통해 작업, 기능 또는 프로젝트에 대한 개발 예측을 제공합니다.
**활용 시기**:
- 시간, 노력 또는 복잡성 예측이 필요한 개발 계획이 있을 때.
- 프로젝트 범위 지정 및 리소스 할당 결정이 필요할 때.
- 체계적인 예측 방법론이 필요한 기능 세분화가 있을 때.
- 위험 평가 및 신뢰 구간 분석 요구 사항이 있을 때.
**예시**:
- **기능 개발 예측**: `/sc:estimate "user authentication system" --type time --unit days --breakdown` (데이터베이스 설계 (2일) + 백엔드 API (3일) + 프론트엔드 UI (2일) + 테스트 (1일)에 대한 체계적인 분석을 수행하고 85% 신뢰 구간으로 총 8일을 예측합니다.)
- **프로젝트 복잡성 평가**: `/sc:estimate "migrate monolith to microservices" --type complexity --breakdown` (위험 요소 및 종속성 매핑을 포함하는 아키텍처 복잡성 분석을 수행합니다.)
- **성능 최적화 노력**: `/sc:estimate "optimize application performance" --type effort --unit hours` (벤치마크 비교를 통해 성능 페르소나 분석을 수행하고 최적화 범주 및 예상 영향별로 노력 세분화를 제공합니다.)

### 2.9 `/sc:explain` - 코드 및 개념 설명

**설명**: 교육적 명확성을 통해 코드, 개념 및 시스템 동작에 대한 명확한 설명을 제공합니다.
**활용 시기**:
- 복잡한 기능에 대한 코드 이해 및 문서 요청이 있을 때.
- 아키텍처 구성 요소에 대한 시스템 동작 설명이 필요할 때.
- 지식 전달을 위한 교육 콘텐츠 생성이 필요할 때.
- 프레임워크별 개념 명확화 요구 사항이 있을 때.
**예시**:
- **기본 코드 설명**: `/sc:explain authentication.js --level basic` (초보자를 위한 실용적인 예시와 함께 명확한 설명을 제공합니다.)
- **프레임워크 개념 설명**: `/sc:explain react-hooks --level intermediate --context react` (공식 React 문서 패턴에 대한 Context7 통합을 통해 점진적인 복잡성을 가진 구조화된 설명을 제공합니다.)
- **시스템 아키텍처 설명**: `/sc:explain microservices-system --level advanced --format interactive` (아키텍처 페르소나가 시스템 설계 및 패턴을 설명하고 순차적 분석 세분화를 통해 대화형 탐색을 제공합니다.)

### 2.10 `/sc:git` - Git 작업

**설명**: 지능형 커밋 메시지 및 워크플로우 최적화를 통해 Git 작업을 수행합니다.
**활용 시기**:
- Git 저장소 작업: status, add, commit, push, pull, branch가 필요할 때.
- 지능형 커밋 메시지 생성이 필요할 때.
- 저장소 워크플로우 최적화 요청이 있을 때.
- 브랜치 관리 및 병합 작업이 필요할 때.
**예시**:
- **스마트 상태 분석**: `/sc:git status` (변경 요약과 함께 저장소 상태를 분석하고 다음 단계 및 워크플로우 권장 사항을 제공합니다.)
- **지능형 커밋**: `/sc:git commit --smart-commit` (변경 분석을 통해 컨벤셔널 커밋 메시지를 생성하고 모범 사례 및 일관된 형식을 적용합니다.)
- **대화형 작업**: `/sc:git merge feature-branch --interactive` (충돌 해결 지원을 통해 안내식 병합을 수행합니다.)

### 2.11 `/sc:help` - 명령 참조 문서

**설명**: 사용 가능한 모든 `/sc` 명령 및 해당 기능을 나열합니다.
**활용 시기**:
- 사용 가능한 Super Claude 명령을 탐색하고 해당 기능을 이해해야 할 때.
- 특정 명령의 사용법이나 기능에 대한 도움이 필요할 때.
- Super Claude 프레임워크의 전체 기능을 빠르게 확인하고 싶을 때.
**예시**:
- **모든 Super Claude 명령 표시**: `/sc:help` (사용 가능한 모든 Super Claude 명령과 설명이 포함된 포괄적인 목록을 표시합니다.)

### 2.12 `/sc:implement` - 기능 구현

**설명**: 지능형 페르소나 활성화 및 MCP 통합을 통해 기능 및 코드 구현을 수행합니다.
**활용 시기**:
- 구성 요소, API 또는 완전한 기능에 대한 기능 개발 요청이 있을 때.
- 프레임워크별 요구 사항이 있는 코드 구현이 필요할 때.
- 조정된 전문 지식이 필요한 다중 도메인 개발이 있을 때.
- 테스트 및 유효성 검사 통합이 필요한 구현 프로젝트가 있을 때.
**예시**:
- **React 구성 요소 구현**: `/sc:implement user profile component --type component --framework react` (Magic MCP가 디자인 시스템 통합이 포함된 UI 구성 요소를 생성하고 프론트엔드 페르소나가 모범 사례 및 접근성을 보장합니다.)
- **API 서비스 구현**: `/sc:implement user authentication API --type api --safe --with-tests` (백엔드 페르소나가 서버 측 로직 및 데이터 처리를 처리하고 보안 페르소나가 인증 모범 사례를 보장합니다.)
- **풀스택 기능**: `/sc:implement payment processing system --type feature --with-tests` (아키텍처, 프론트엔드, 백엔드, 보안 페르소나의 다중 페르소나 조정이 복잡한 구현 단계를 세분화합니다.)

### 2.13 `/sc:improve` - 코드 개선

**설명**: 코드 품질, 성능 및 유지 관리 가능성에 체계적인 개선을 적용합니다.
**활용 시기**:
- 코드 품질 향상 및 리팩토링 요청이 있을 때.
- 성능 최적화 및 병목 현상 해결이 필요할 때.
- 유지 관리 가능성 개선 및 기술 부채 감축이 필요할 때.
- 모범 사례 적용 및 코딩 표준 시행이 필요할 때.
**예시**:
- **코드 품질 향상**: `/sc:improve src/ --type quality --safe` (안전한 리팩토링 적용과 함께 체계적인 품질 분석을 수행하고 코드 구조를 개선하고 기술 부채를 줄이며 가독성을 높입니다.)
- **성능 최적화**: `/sc:improve api-endpoints --type performance --interactive` (성능 페르소나가 병목 현상 및 최적화 기회를 분석하고 복잡한 성능 개선 결정에 대한 대화형 지침을 제공합니다.)
- **유지 관리 가능성 개선**: `/sc:improve legacy-modules --type maintainability --preview` (아키텍처 페르소나가 구조를 분석하고 유지 관리 가능성 개선을 제안합니다.)

### 2.14 `/sc:index` - 프로젝트 문서화

**설명**: 지능형 구성을 통해 포괄적인 프로젝트 문서 및 지식 기반을 생성합니다.
**활용 시기**:
- 프로젝트 문서 생성 및 유지 관리 요구 사항이 있을 때.
- 지식 기반 생성 및 구성이 필요할 때.
- API 문서 및 구조 분석 요구 사항이 있을 때.
- 교차 참조 및 탐색 개선 요청이 있을 때.
**예시**:
- **프로젝트 구조 문서화**: `/sc:index project-root --type structure --format md` (지능형 구성을 통해 포괄적인 프로젝트 구조 문서를 생성하고 교차 참조 및 구성 요소 관계가 포함된 탐색 가능한 구조를 생성합니다.)
- **API 문서 생성**: `/sc:index src/api --type api --format json` (체계적인 분석 및 유효성 검사를 통해 API 문서를 생성합니다.)
- **지식 기반 생성**: `/sc:index . --type docs` (프로젝트별 패턴이 포함된 대화형 지식 기반을 생성합니다.)

### 2.15 `/sc:index-repo` - 저장소 인덱싱

**설명**: 저장소의 핵심 구조, 코드 구성, 종속성 및 기타 중요한 메타데이터를 분석하고, `PROJECT_INDEX.md` 및 `PROJECT_INDEX.json` 파일에 요약된 인덱스를 생성합니다. 이 인덱스는 대규모 코드베이스에서 Super Claude의 컨텍스트 이해를 돕고, 토큰 사용량을 획기적으로 줄여줍니다.
**활용 시기**:
- 프로젝트의 전체적인 구조와 핵심 구성 요소를 빠르게 파악해야 할 때.
- Super Claude가 대규모 저장소에서 작업할 때 컨텍스트를 효율적으로 유지하도록 돕고 싶을 때.
- 반복적인 Super Claude 세션에서 토큰 사용량과 응답 시간을 최적화하고 싶을 때.
- 프로젝트의 진입점, 주요 모듈, 구성 파일 및 테스트 커버리지를 요약된 형식으로 보고 싶을 때.
**예시**:
- **저장소 인덱스 생성**: `/sc:index-repo` (현재 저장소의 구조를 분석하고 `PROJECT_INDEX.md` 및 `PROJECT_INDEX.json` 파일을 생성하여 핵심 정보를 요약합니다.)
- **기존 인덱스 업데이트**: `/sc:index-repo mode=update` (새로운 변경 사항을 반영하기 위해 기존 저장소 인덱스를 업데이트합니다.)
- **빠른 인덱스 생성 (테스트 스킵)**: `/sc:index-repo mode=quick` (테스트 분석을 건너뛰고 핵심 코드 및 문서 구조에 대한 빠른 인덱스를 생성합니다.)

### 2.16 `/sc:load` - 프로젝트 컨텍스트 로딩

**설명**: 프로젝트 컨텍스트를 로드하여 세션을 초기화하고 Serena MCP 통합을 통해 교차 세션 지속성을 제공합니다.
**활용 시기**:
- 세션 초기화 및 프로젝트 컨텍스트 로딩 요청이 있을 때.
- 교차 세션 지속성 및 메모리 검색이 필요할 때.
- 프로젝트 활성화 및 컨텍스트 관리 요구 사항이 있을 때.
- 세션 수명 주기 관리 및 체크포인트 로딩 시나리오가 있을 때.
**예시**:
- **기본 프로젝트 로딩**: `/sc:load` (Serena 메모리 통합을 통해 현재 디렉토리 프로젝트 컨텍스트를 로드하고 세션 컨텍스트를 설정하며 개발 워크플로우를 준비합니다.)
- **특정 프로젝트 로딩**: `/sc:load /path/to/project --type project --analyze` (포괄적인 분석을 통해 특정 프로젝트를 로드하고 프로젝트 컨텍스트를 활성화하며 교차 세션 메모리를 검색합니다.)
- **체크포인트 복원**: `/sc:load --type checkpoint --checkpoint session_123` (세션 컨텍스트와 함께 특정 체크포인트를 복원하고 전체 컨텍스트 보존과 함께 이전 작업 세션을 계속합니다.)

### 2.17 `/sc:pm` - 프로젝트 관리자 에이전트 (항상 활성)

**설명**: 모든 하위 에이전트를 조정하고 워크플로우를 원활하게 관리하는 기본 오케스트레이션 에이전트입니다. 사용자가 수동으로 호출할 필요가 없으며, 세션 간 지속적인 컨텍스트를 통해 모든 상호 작용을 자동으로 오케스트레이션합니다.
**활용 시기**:
- **모든 사용자 요청의 기본 진입점**: PM Agent는 사용자가 명시적으로 다른 하위 에이전트를 지정하지 않는 한 모든 상호 작용을 처리합니다.
- **세션 시작 시**: Serena MCP 메모리를 통해 이전 컨텍스트를 자동으로 복원하고 현재 진행 상황 및 다음 단계를 사용자에게 보고합니다.
- **모호한 요청**: 사용자의 요청이 모호할 때 (예: "만들고 싶어요", "구현하고 싶어요") 요구사항 발견 모드를 트리거합니다.
- **다중 도메인 작업**: 여러 전문가의 조율이 필요한 교차 기능 작업을 처리합니다.
- **복잡한 프로젝트**: 체계적인 계획 및 PDCA (Plan-Do-Check-Act) 사이클 실행이 필요한 경우.
**예시**:
- **기본 사용법 (명령어 필요 없음)**: 사용자가 "앱에 인증 기능 만들고 싶어"와 같이 요청하면, PM Agent는 자동으로 요구사항 분석, 아키텍처 설계, 보안 엔지니어링, 백엔드 구현, 품질 검증 및 문서화에 이르기까지 전체 워크플로우를 조율합니다.
- **명시적 전략 선택**: `/sc:pm "애플리케이션 보안 강화" --strategy wave` (보안 감사를 위한 Wave 모드를 시작하여 보안 엔지니어, 백엔드 아키텍트, 품질 엔지니어를 단계별로 참여시킵니다.)
- **브레인스토밍 모드**: 사용자가 "사용자 경험을 개선할 수 있을까?"라고 질문하면, PM Agent는 브레인스토밍 모드를 활성화하여 소크라테스식 질문을 통해 요구사항을 탐색하고 개선 계획을 수립합니다.

### 2.18 `/sc:reflect` - 작업 반영 및 유효성 검사

**설명**: Serena MCP 분석 기능을 사용하여 작업 반영 및 유효성 검사를 수행합니다.
**활용 시기**:
- 유효성 검사 및 품질 평가가 필요한 작업 완료 시.
- 세션 진행 상황 분석 및 완료된 작업에 대한 반영이 필요할 때.
- 프로젝트 개선을 위한 교차 세션 학습 및 통찰력 캡처가 필요할 때.
- 포괄적인 작업 준수 검증이 필요한 품질 게이트가 있을 때.
**예시**:
- **작업 준수 반영**: `/sc:reflect --type task --analyze` (프로젝트 목표에 대한 현재 접근 방식을 검증하고 편차를 식별하며 수정 권장 사항을 제공합니다.)
- **세션 진행 상황 분석**: `/sc:reflect --type session --validate` (세션 작업 및 정보 수집 완료에 대한 포괄적인 분석을 수행하고 프로젝트 개선을 위한 품질 평가 및 격차 식별을 제공합니다.)
- **완료 유효성 검사**: `/sc:reflect --type completion` (실제 진행 상황에 대한 작업 완료 기준을 평가하고 작업 완료 준비 상태를 결정하며 남아 있는 차단 요소를 식별합니다.)

### 2.19 `/sc:recommend` - 지능형 명령 추천 엔진

**설명**: 모든 사용자 입력에 대해 가장 적합한 Super Claude 명령을 추천하는 초지능형 명령 추천 엔진입니다.
**활용 시기**:
- 어떤 Super Claude 명령을 사용해야 할지 모를 때.
- 복잡한 작업에 대한 최적의 워크플로우를 찾고 싶을 때.
- 특정 기술 스택이나 요구사항에 맞는 추천을 받고 싶을 때.
- 프로젝트 진행 상황에 따라 다음 단계를 추천받고 싶을 때.
**예시**:
- **새로운 React 프로젝트 추천**: `/sc:recommend "새로운 React 프로젝트를 만들고 싶어요"` (React 프로젝트 생성에 가장 적합한 `/sc:build --magic --react`와 같은 명령을 추천합니다.)
- **웹 성능 최적화 추천**: `/sc:recommend "제 웹사이트가 너무 느려요"` (성능 분석 및 개선에 필요한 `/sc:analyze --performance`, `/sc:improve --performance` 명령을 추천합니다.)
- **API 보안 강화 추천 (고급)**: `/sc:recommend "인증 및 권한 부여로 API를 보호해야 합니다"` (보안 분석, 스캔 및 설계에 대한 명령을 추천합니다.)
- **대안 및 예상 시간 포함**: `/sc:recommend "새로운 블로그 사이트" --estimate --alternatives --community` (블로그 사이트 구축에 대한 다양한 기술 스택 대안, 예상 시간 및 커뮤니티 선호도를 포함한 포괄적인 추천을 제공합니다.)

### 2.20 `/sc:research` - 심층 연구 명령

**설명**: 적응형 계획 및 지능형 검색을 통해 심층 웹 연구를 수행합니다.
**활용 시기**:
- 지식 차단 기간을 넘어선 연구 질문이 있을 때.
- 복잡한 연구 질문이 있을 때.
- 시사 및 실시간 정보가 필요할 때.
- 학술 또는 기술 연구 요구 사항이 있을 때.
- 시장 분석 및 경쟁 정보가 필요할 때.
**예시**:
- **양자 컴퓨팅 최신 개발 동향 연구**: `/sc:research "latest developments in quantum computing 2024"`
- **AI 코딩 어시스턴트의 경쟁 분석 (심층)**: `/sc:research "competitive analysis of AI coding assistants" --depth deep`
- **분산 시스템 모범 사례 연구 (통합 전략)**: `/sc:research "best practices for distributed systems" --strategy unified`

### 2.21 `/sc:save` - 세션 컨텍스트 지속성

**설명**: Serena MCP 통합을 통해 세션 컨텍스트 지속성을 위한 세션 수명 주기 관리를 수행합니다.
**활용 시기**:
- 세션 완료 및 프로젝트 컨텍스트 지속성이 필요할 때.
- 교차 세션 메모리 관리 및 체크포인트 생성이 필요할 때.
- 프로젝트 이해 보존 및 발견 사항 아카이브가 필요할 때.
- 세션 수명 주기 관리 및 진행 상황 추적 요구 사항이 있을 때.
**예시**:
- **기본 세션 저장**: `/sc:save` (현재 세션 발견 사항 및 컨텍스트를 Serena MCP에 저장하고 세션이 30분을 초과하면 자동으로 체크포인트를 생성합니다.)
- **포괄적인 세션 체크포인트**: `/sc:save --type all --checkpoint` (세션 복원을 위한 복구 체크포인트와 함께 완전한 세션 보존을 수행합니다. 모든 학습, 컨텍스트 및 진행 상황을 포함합니다.)
- **발견 사항만 지속**: `/sc:save --type learnings` (세션 중에 발견된 새로운 패턴 및 통찰력만 저장하고 전체 세션 보존 없이 프로젝트 이해를 업데이트합니다.)

### 2.22 `/sc:select-tool` - 지능형 MCP 도구 선택

**설명**: 복잡성 점수 및 작업 분석을 기반으로 지능형 MCP 도구 선택을 수행합니다.
**활용 시기**:
- Serena와 Morphllm 간에 최적의 MCP 도구 선택이 필요한 작업이 있을 때.
- 복잡성 분석 및 기능 일치가 필요한 메타 시스템 결정이 있을 때.
- 성능과 정확성 간의 균형이 필요한 도구 라우팅 결정이 있을 때.
- 지능형 도구 기능 평가의 이점을 얻는 작업이 있을 때.
**예시**:
- **복잡한 리팩토링 작업**: `/sc:select-tool "rename function across 10 files" --analyze` (높은 복잡성 작업에 대해 Serena MCP를 선택합니다.)
- **패턴 기반 대량 편집**: `/sc:select-tool "update console.log to logger.info across project" --explain` (패턴 기반 변환에 대해 Morphllm MCP를 선택합니다.)
- **메모리 관리 작업**: `/sc:select-tool "save project context and discoveries"` (메모리 작업에 대해 Serena MCP를 선택합니다.)

### 2.23 `/sc:spawn` - 메타 시스템 작업 오케스트레이션

**설명**: 지능형 세분화 및 위임을 통해 메타 시스템 작업을 오케스트레이션합니다.
**활용 시기**:
- 복잡한 작업에 대한 메타 시스템 작업 오케스트레이션이 필요할 때.
- 다중 도메인 작업에 대한 지능형 세분화 및 위임이 필요할 때.
- 체계적인 실행 및 진행 상황 추적을 통한 자동화된 워크플로우가 필요할 때.
- 동적 컨텍스트 관리를 통한 교차 세션 지속성이 필요할 때.
**예시**:
- **대규모 기능 구현**: `/sc:spawn "새로운 결제 시스템 구축"` (프로젝트 관리자, 백엔드, 보안, 프론트엔드 등 다양한 페르소나에게 작업을 지능적으로 세분화하고 위임하여 복잡한 결제 시스템 구축 작업을 오케스트레이션합니다.)
- **기술 부채 감축 프로그램**: `/sc:spawn "레거시 코드베이스 리팩토링" --strategy systematic --parallel` (코드 분석, 리팩토링, 테스트 및 문서화 작업을 여러 에이전트에 병렬로 위임하여 기술 부채 감축 프로그램을 체계적으로 관리합니다.)
- **새로운 서비스 출시 워크플로우**: `/sc:spawn "신규 마이크로서비스 출시" --strategy enterprise --validate` (요구사항 정의부터 배포 및 모니터링까지 전체 출시 워크플로우를 관리하고 각 단계에서 필요한 전문가 페르소나 및 MCP 서버를 동적으로 할당합니다.)

### 2.24 `/sc:spec-panel` - 전문가 사양 검토 패널

**설명**: 저명한 사양 및 소프트웨어 엔지니어링 전문가를 사용하여 다중 전문가 사양 검토 및 개선을 수행합니다.
**활용 시기**:
- 사양 품질 검토 및 개선 요청이 있을 때.
- 기술 문서 유효성 검사 및 향상 요구 사항이 있을 때.
- 요구 사항 분석 및 완전성 검증이 필요할 때.
- 전문 사양 작성 지침 및 멘토링이 필요할 때.
**예시**:
- **API 사양 검토**: `/sc:spec-panel @auth_api.spec.yml --mode critique --focus requirements,architecture` (요구 사항 품질 및 아키텍처 일관성에 중점을 두고 포괄적인 API 사양 검토를 수행합니다.)
- **요구 사항 워크숍**: `/sc:spec-panel "user story content" --mode discussion --experts "wiegers,adzic,cockburn"` (요구 사항 개선을 위한 전문가 대화와 함께 협업 요구 사항 분석을 수행합니다.)
- **아키텍처 유효성 검사**: `/sc:spec-panel @microservice.spec.yml --mode socratic --focus architecture` (설계 결정에 대한 심층적인 질문과 함께 학습 중심 아키텍처 검토를 수행합니다.)

### 2.25 `/sc:task` - 향상된 작업 관리

**설명**: 지능형 워크플로우 관리 및 위임을 통해 복잡한 작업을 실행합니다.
**활용 시기**:
- 다중 에이전트 조정 및 위임이 필요한 복잡한 작업이 있을 때.
- 구조화된 워크플로우 관리 및 교차 세션 지속성이 필요한 프로젝트가 있을 때.
- 지능형 MCP 서버 라우팅 및 도메인 전문 지식이 필요한 작업이 있을 때.
- 체계적인 실행 및 점진적인 개선의 이점을 얻는 작업이 있을 때.
**예시**:
- **복잡한 기능 개발**: `/sc:task create "enterprise authentication system" --strategy systematic --parallel` (다중 도메인 조정과 함께 포괄적인 작업 세분화를 수행합니다. 아키텍처, 보안, 백엔드, 프론트엔드 페르소나를 활성화합니다.)
- **애자일 스프린트 조정**: `/sc:task execute "feature backlog" --strategy agile --delegate` (지능형 위임과 함께 반복적인 작업 실행을 수행하고 스프린트 연속성을 위한 교차 세션 지속성을 제공합니다.)
- **다중 도메인 통합**: `/sc:task execute "microservices platform" --strategy enterprise --parallel` (규정 준수 유효성 검사와 함께 엔터프라이즈 규모 조정을 수행하고 여러 기술 도메인에 걸쳐 병렬 실행을 제공합니다.)

### 2.26 `/sc:test` - 테스트 및 품질 보증

**설명**: 커버리지 분석 및 자동화된 품질 보고를 통해 테스트를 실행합니다.
**활용 시기**:
- 단위, 통합 또는 E2E 테스트에 대한 테스트 실행 요청이 있을 때.
- 커버리지 분석 및 품질 게이트 유효성 검사가 필요할 때.
- 지속적인 테스트 및 watch 모드 시나리오가 있을 때.
- 테스트 실패 분석 및 디버깅 요구 사항이 있을 때.
**예시**:
- **기본 테스트 실행**: `/sc:test` (표준 구성으로 모든 테스트를 검색하고 실행하며 통과/실패 요약 및 기본 커버리지를 생성합니다.)
- **대상 커버리지 분석**: `/sc:test src/components --type unit --coverage` (상세한 커버리지 메트릭이 포함된 특정 디렉토리에 대한 단위 테스트를 수행합니다.)
- **브라우저 테스트**: `/sc:test --type e2e` (포괄적인 브라우저 테스트를 위해 Playwright MCP를 활성화합니다. 교차 브라우저 호환성 및 시각적 유효성 검사를 제공합니다.)

### 2.27 `/sc:troubleshoot` - 문제 진단 및 해결

**설명**: 코드, 빌드, 배포 및 시스템 동작 문제를 진단하고 해결합니다.
**활용 시기**:
- 코드 결함 및 런타임 오류 조사 요청이 있을 때.
- 빌드 실패 분석 및 해결이 필요할 때.
- 성능 문제 진단 및 최적화 요구 사항이 있을 때.
- 배포 문제 분석 및 시스템 동작 디버깅이 필요할 때.
**예시**:
- **코드 버그 조사**: `/sc:troubleshoot "Null pointer exception in user service" --type bug --trace` (오류 컨텍스트 및 스택 추적에 대한 체계적인 분석을 수행하고 근본 원인을 식별하며 대상 수정 권장 사항을 제공합니다.)
- **빌드 실패 분석**: `/sc:troubleshoot "TypeScript compilation errors" --type build --fix` (빌드 로그 및 TypeScript 구성을 분석하고 일반적인 컴파일 문제에 대한 안전한 수정을 자동으로 적용합니다.)
- **성능 문제 진단**: `/sc:troubleshoot "API response times degraded" --type performance` (성능 메트릭 분석 및 병목 현상 식별을 수행하고 최적화 권장 사항 및 모니터링 지침을 제공합니다.)

### 2.28 `/sc:workflow` - 구현 워크플로우 생성기

**설명**: PRD (제품 요구 사항 문서) 및 기능 요구 사항에서 구조화된 구현 워크플로우를 생성합니다.
**활용 시기**:
- PRD 및 기능 사양 분석을 통해 구현 계획을 수립해야 할 때.
- 개발 프로젝트에 대한 구조화된 워크플로우 생성이 필요할 때.
- 복잡한 구현 전략에 대한 다중 페르소나 조정이 필요할 때.
- 교차 세션 워크플로우 관리 및 종속성 매핑이 필요할 때.
**예시**:
- **체계적인 PRD 워크플로우**: `/sc:workflow Claudedocs/PRD/feature-spec.md --strategy systematic --depth deep` (체계적인 워크플로우 생성과 함께 포괄적인 PRD 분석을 수행하고 완전한 구현 전략을 위한 다중 페르소나 조정을 제공합니다.)
- **애자일 기능 워크플로우**: `/sc:workflow "user authentication system" --strategy agile --parallel` (병렬 작업 조정과 함께 애자일 워크플로우 생성을 수행하고 프레임워크 및 UI 워크플로우 패턴에 대한 Context7 및 Magic MCP를 제공합니다.)
- **엔터프라이즈 구현 계획**: `/sc:workflow enterprise-prd.md --strategy enterprise --validate` (포괄적인 유효성 검사와 함께 엔터프라이즈 규모 워크플로우를 수행하고 규정 준수 및 확장성을 위한 보안, devops 및 아키텍처 페르소나를 제공합니다.)

---

## 3. Super Claude 프레임워크 플래그 (Flags)

Super Claude는 특정 실행 모드 및 도구 선택 패턴을 활성화하는 동작 플래그를 지원합니다. 이러한 플래그는 `/sc` 명령과 함께 사용하여 동작을 사용자 지정할 수 있습니다.

### 3.1 모드 활성화 플래그

| 플래그 | 트리거 | 동작 |
|---|---|---|
| `--brainstorm` | 모호한 프로젝트 요청, 탐색 키워드 | 협업적 발견 사고방식 활성화, 탐색 질문 |
| `--introspect` | 자기 분석 요청, 오류 복구 | 투명성 마커로 사고 과정 노출 |
| `--task-manage` | 다단계 작업 (>3단계) | 위임, 체계적인 구성을 통해 조율 |
| `--orchestrate` | 다중 도구 작업, 병렬 실행 | 도구 선택 매트릭스 최적화, 병렬 사고 활성화 |
| `--token-efficient` | 컨텍스트 사용량 >75%, 대규모 작업 | 심볼 기반 통신, 30-50% 토큰 감소 |

### 3.2 MCP 서버 플래그

| 플래그 | 트리거 | 동작 |
|---|---|---|
| `--c7` / `--context7` | 라이브러리 import, 프레임워크 질문 | Context7을 활성화하여 선별된 문서 조회 |
| `--seq` / `--sequential` | 복잡한 디버깅, 시스템 설계 | Sequential을 활성화하여 구조화된 다단계 추론 |
| `--magic` | UI 구성 요소 요청 (/ui, /21) | Magic을 활성화하여 21st.dev 패턴에서 현대적인 UI 생성 |
| `--morph` / `--morphllm` | 대량 코드 변환 | Morphllm을 활성화하여 효율적인 다중 파일 패턴 적용 |
| `--serena` | 심볼 작업, 프로젝트 메모리 | Serena를 활성화하여 의미 이해 및 세션 지속성 |
| `--play` / `--playwright` | 브라우저 테스트, E2E 시나리오 | Playwright를 활성화하여 실제 브라우저 자동화 및 테스트 |
| `--all-mcp` | 최대 복잡성 시나리오 | 모든 MCP 서버를 활성화하여 포괄적인 기능 |
| `--no-mcp` | 기본 도구만 필요한 경우 | 모든 MCP 서버 비활성화, 기본 도구 사용 |

### 3.3 분석 깊이 플래그

| 플래그 | 트리거 | 동작 |
|---|---|---|
| `--think` | 다중 구성 요소 분석 | 표준 구조화된 분석 (~4K 토큰), Sequential 활성화 |
| `--think-hard` | 아키텍처 분석, 시스템 전반의 종속성 | 심층 분석 (~10K 토큰), Sequential + Context7 활성화 |
| `--ultrathink` | 중요 시스템 재설계, 레거시 현대화 | 최대 심층 분석 (~32K 토큰), 모든 MCP 서버 활성화 |

### 3.4 실행 제어 플래그

| 플래그 | 트리거 | 동작 |
|---|---|---|
| `--delegate [auto\|files\|folders]` | >7개 디렉토리 또는 >50개 파일 | 지능형 라우팅을 통해 하위 에이전트 병렬 처리 활성화 |
| `--concurrency [n]` | 리소스 최적화 | 최대 동시 작업 제어 (범위: 1-15) |
| `--loop` | 개선 키워드 (다듬기, 개선, 향상) | 유효성 검사 게이트를 통한 반복적인 개선 주기 활성화 |
| `--iterations [n]` | 특정 개선 주기 요구 사항 | 개선 주기 횟수 설정 (범위: 1-10) |
| `--validate` | 위험 점수 >0.7, 리소스 사용량 >75% | 사전 실행 위험 평가 및 유효성 검사 게이트 |
| `--safe-mode` | 리소스 사용량 >85%, 프로덕션 환경 | 최대 유효성 검사, 보수적인 실행 |

### 3.5 출력 최적화 플래그

| 플래그 | 트리거 | 동작 |
|---|---|---|
| `--uc` / `--ultracompressed` | 컨텍스트 압력, 효율성 요구 사항 | 심볼 통신 시스템, 30-50% 토큰 감소 |
| `--scope [file\|module\|project\|system]` | 분석 경계 | 작업 범위 및 분석 깊이 정의 |
| `--focus [performance\|security\|quality\|architecture\|accessibility\|testing]` | 도메인별 최적화 | 특정 분석 도메인 및 전문 지식 적용 |

### 3.6 플래그 우선순위 규칙

- **안전 우선**: `--safe-mode` > `--validate` > 최적화 플래그
- **명시적 재정의**: 사용자 플래그 > 자동 감지
- **깊이 계층**: `--ultrathink` > `--think-hard` > `--think`
- **MCP 제어**: `--no-mcp`는 모든 개별 MCP 플래그를 재정의
- **범위 우선순위**: system > project > module > file

---

## 4. 결론

Super Claude CLI는 복잡한 소프트웨어 개발 과제를 해결하기 위한 강력하고 다재다능한 도구입니다. 위에 설명된 다양한 명령과 플래그를 이해하고 적절히 활용하면, 코드 품질을 향상시키고, 개발 프로세스를 가속화하며, 궁극적으로 더 나은 소프트웨어 제품을 만들 수 있습니다. Super Claude를 최대한 활용하여 프로젝트를 성공으로 이끄시길 바랍니다.
