# Technical Requirements Document (TRD) v2.0
## Project: DevOps & Git CLI Cheatsheet Expansion
**Date**: 2025-12-05
**Author**: Gemini (Tech Lead)

### 1. Architecture
Reuse the existing `platform` server logic. No code changes required, only content addition.

#### 1.1 Content Directory Structure
Ensure ASCII naming to prevent encoding issues.
```text
content/
|-- tools/
    |-- docker/
        |-- Step1_Docker_Cheatsheet.md
    |-- kubectl/
        |-- Step1_Kubectl_Cheatsheet.md
    |-- git/
        |-- Step1_Git_Cheatsheet.md
```

### 2. Implementation Details

#### 2.1 Docker Cheatsheet
-   **Categories**: Lifecycle, Images, Network, Volumes.
-   **Key Commands**: `docker run -d -p`, `docker ps -a`, `docker exec -it`, `docker logs -f`.

#### 2.2 Kubectl Cheatsheet
-   **Categories**: Diagnosis, Resources, Configuration.
-   **Key Commands**: `kubectl get pods -o wide`, `kubectl logs -f <pod>`, `kubectl port-forward`.

#### 2.3 Git Cheatsheet
-   **Categories**: Setup, Snapshot, Branch & Merge, Inspect & Compare.
-   **Key Commands**: `git commit -m`, `git switch -c`, `git log --oneline --graph`.

### 3. Verification Plan (GCX Protocol)
1.  **Gemini**: Create files using `write_file` (UTF-8 safe).
2.  **Claude**: Quality Check (Content accuracy).
3.  **Codex**: Audit (Encoding check via `[System.IO.File]`).
